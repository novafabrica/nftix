<p id="notice"><%= notice %></p>

<h2><%= @ticket_group.name %></h2>

<p>
  <strong>Ticket count:</strong>
  <%= @ticket_group.tickets_count %>
</p>


<%= link_to 'Edit', edit_ticket_group_path(@ticket_group) %> |
<%= link_to 'Back', ticket_groups_path %>

<h3>tickets</h3>
<%= render :partial => 'application/tickets', :locals => {:tickets => @tickets} %>
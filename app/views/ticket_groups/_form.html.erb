<%= form_for(@ticket_group) do |f| %>
  <% if @ticket_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_group.errors.count, "error") %> prohibited this ticket_group from being saved:</h2>

      <ul>
      <% @ticket_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :position %>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :active %>
    <%= f.check_box :active %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ticket_group_id %>
    <%= f.collection_select :ticket_group_id, TicketGroup.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :owner_id %>
    <%= f.collection_select :owner_id, User.all, :id, :full_name %>
  </div>
    <div class="field">
    <%= f.label :creator_id %>
    <%= f.collection_select :creator_id, User.all, :id, :full_name %>
  </div>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :status %>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :due_date %>
    <%= f.date_field :due_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
